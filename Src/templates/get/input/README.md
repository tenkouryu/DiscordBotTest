# Get Input CSV

`chat_template.csv` is the CSV input for `/chat get` when retrieving attachments from multiple channels.

Generated Get outputs are stored under `../result/` and returned as Discord attachments.