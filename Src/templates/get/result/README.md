# Get Results

CSV and ZIP outputs from Get/export commands are generated from the current Discord server, retained in this folder, and returned as Discord attachments. Output columns may vary by command; for example, server role exports include the permissions supported by the current Discord API. Generated files are excluded from Git because they can contain server data.